# カードバトル 仕様書

最終更新：2026-08-23

図鑑アプリ「【claude-code】キャラクター図鑑」で登録したキャラクターを使って対戦するカードゲーム。

**公開URL：https://ske80225-lab.github.io/card-battle/**

## ファイル構成

| ファイル | 内容 |
|---|---|
| `index.html` | 本体。HTML・CSS・JS すべて同梱 |
| `CB_title@2x.png` | 画面左上のタイトルロゴ（790×243の@2x画像） |
| `icon_thum.png` | ホーム画面に追加したときのアイコン（500×500） |
| `仕様.md` | このファイル |

デプロイは `~/deploy-gh-pages.sh card-battle` を実行する（リポジトリ `ske80225-lab/card-battle`、public）。

---

## 1. データの取得元

キャラクターデータは図鑑と**同じ Firebase Firestore** を直接読み込む。バトル側にキャラ情報は一切持たない。

| 項目 | 値 |
|---|---|
| projectId | `riku-zukan` |
| コレクション | `characters` |
| 並び順 | `createdAt` 昇順（失敗時は並び順なしで再取得） |
| 画像 | 図鑑が保存した `imageUrl`（Cloudinary URL または data URI）をそのまま表示 |

図鑑にキャラを追加・編集すると、バトル側は**リロードするだけで反映**される。サイトの再デプロイは不要（再デプロイが必要なのは `index.html` 自体を変更したときだけ）。バトル側からの書き込みは一切行わない（読み取り専用）。

### 使用するフィールド

| Firestore | バトルでの用途 |
|---|---|
| `name` | キャラ名 |
| `no` | デッキ選択画面の「No.◯」表示 |
| `imageUrl` | カードの絵。空なら 🃏 を表示 |
| `maxHp` | バトルのHP（図鑑側の入力範囲 10〜500） |
| `moves[]` | わざ。`{ name, damage }` を最大4つ。`name` が空のものは無視 |

`atk` / `hp` / `def` / `spd` / `int`（図鑑の5ステータス）は**バトルでは使わない**。図鑑の表示専用。

### バトルに出せる条件

`maxHp > 0` かつ `わざが1つ以上` のキャラのみ「使える」と判定する（`playable`）。
条件を満たさないキャラはデッキ選択画面で淡色表示になり、選択できない。
図鑑でHPとわざを入力すれば自動的に選べるようになる。

---

## 2. デッキ選択（交互ドラフト）

バトル開始前に必ずこの画面から始まる。

- **1人がデッキ20体**。すべてキャラクターカード（アイテムカードは現在なし）
- **交互に1体ずつ**選ぶ。プレイヤー1 → 相手 → プレイヤー1 → … の繰り返し
- **同じキャラは1人につき2体まで**（`MAX_COPIES = 2`）。3体目を選ぼうとすると淡色になり「もう2体えらんでいます」と表示される
- **相手とかぶってもOK**。相手が選んだキャラも、自分の枠で2体まで選べる
- 選択済みのキャラにはバッジが付く（`P1×2` / `CPU×1` / `P2×1`）。カードの枠色は P1＝赤、P2/CPU＝青
- 両者が20体そろった時点で自動的にバトル開始

### デッキ枚数の自動調整

使えるキャラ数 × 2 が20に満たない場合、デッキ枚数を自動的に減らす（`deckSize()`）。
図鑑の入力が進んでいない段階でもデッキが組めなくならないための安全装置。減った場合は画面に「※使えるキャラが少ないため、デッキは◯体になります。」と表示する。

### 「おまかせ」ボタン

画面上部のヘッダー右側にある。押すと**そのプレイヤーの残りが自動でうまる**（2体上限は守る）。

- 対CPU：押すと自分もCPUも一気にうまり、そのままバトル開始
- 2人対戦：押した人だけうまり、相手にバトンタッチ。相手も「おまかせ」を押せる

選び方はランダム（CPUの自動選択とは別ロジック）。

### カウンターの追従固定

「だれのばん」「両者の選択数（◯ / 20）」「ゲージ」「おまかせボタン」をまとめたヘッダーは `position: sticky` で画面上部に固定される。キャラ一覧をスクロールしても、何体選んだかが常に見える。

### CPUの選び方

「HP ＋ 最大ダメージ × 2」でキャラを採点し、上位40%（最低3体）の中からランダムに1体選ぶ（`cpuChoice()`）。
強いキャラを優先しつつ、毎回同じデッキにはならない。選択には約0.55秒の間を置く。

### 2人交互プレイ

対CPUと同じく交互に1体ずつ選ぶ。画面上部に「プレイヤー1のばん」「プレイヤー2のばん」と交互に表示される。

---

## 3. バトルのルール

### 定数

| 名前 | 値 | 意味 |
|---|---|---|
| `DECK_SIZE` | 20 | 1人ぶんのデッキ枚数 |
| `MAX_COPIES` | 2 | 同じキャラをデッキに入れられる上限 |
| `HAND_START` | 4 | 最初に引く手札の枚数 |
| `MAX_LOSS` | 10 | この回数だけキャラが倒れたら敗北 |

### 流れ

1. デッキをシャッフルし、両者が手札を4枚引く
2. 各プレイヤーが手札から場に出すキャラを1体選ぶ（CPUは自動）
3. ルーレットで先攻・後攻を決める（最初の1回だけ）
4. 以降、交互にターンを行う

### ターンの処理

**自分のターンのパネルをタップ／クリック**するとサイコロ（1〜6）が振られる。専用のボタンはない。パネル下部に「🎲 タップ！」という案内が点滅する。

| 出目 | 結果 |
|---|---|
| 1〜4 | その番号のわざを使う。相手にわざのダメージを与える |
| 1〜4だがその番号のわざがない | 何も起きずターン終了 |
| 5・6 | わざが出せずターン終了 |

ダメージ計算は**わざのダメージ値そのまま**。防御・補正・命中判定はなし。

出目は画面中央のテキストに表示される（振っている間は数字がパラパラ切り替わり、結果は `🎲 3 → 自爆ex！` のように出る）。サイコロの絵は表示しない。

### ダメージの表示

ダメージを受けたキャラの**絵の上に数字が大きくポップアップ**する。ドラクエ風に、飛び出す → 落ちる → 2回バウンド → 消える、という動き。白抜き＋赤フチ＋影で、絵に重なっても読める。あわせてカードが左右に小刻みに揺れる。

自爆の自傷ダメージはフチを**茶色**にして通常ダメージと区別する。

数字を見せるため、わざ発動後に1250msの間を取る。

### KO と敗北

- HPが0になったキャラは場から消え、そのプレイヤーの負けカウントが増える（増える数はカードによって変わる）
  - 名前に `ex` または `EX` が付くカード → **+3**
  - キラ背景 `kira_bg.webp`（kira1）→ **+3**
  - キラ背景 `kira_bg2.webp` / `kira_bg3.webp`（kira2・kira3）→ **+2**
  - それ以外 → +1
  - 複数当てはまる場合は大きい方
- 負けたプレイヤーは山札から1枚引き、手札から次のキャラを場に出す
- **新しいキャラを出しただけではターンは終わらない。** 出したあと、そのプレイヤーがサイコロを振って初めてターンが終わる（倒されても行動回数が減らない）
- 負けカウントが `MAX_LOSS`（10）に達したら敗北
- 山札が尽きて出せるキャラがいなくなった場合も敗北

---

## 4. 自爆わざ

**わざの名前に「自爆」「じばく」「ジバク」のいずれかが含まれていれば自爆わざ**として扱う。
完全一致ではなく部分一致なので、「自爆ex」「大自爆」なども自動的に対象になる。図鑑側に新しい自爆わざを登録すればそのまま機能する。

### 処理

1. 通常どおり相手にダメージを与える
2. 続けて**自分のHPも0になる**（「💣 ◯◯の △△ は自爆した！ 自分も消滅する！」とログに出る）
3. 相手も倒れていれば**両者同時KO**
   - 両方の負けカウントがそれぞれのカードに応じて増える
   - 両方が山札から1枚ドローする
   - 順番に次のキャラを場に出す
   - 全員そろったら、攻撃していない側からサイコロを振る
4. 相手が自爆ダメージに耐えた場合は、自分だけが倒れる

### 表示

わざ一覧では自爆わざの前に 💣 が付く（例：`3 💣自爆（400）`）。
デッキ選択画面・手札・バトル画面のすべてで表示されるので、選ぶ前・サイコロを振る前に見分けられる。

### 引き分け

同時KOで両者が同時に `MAX_LOSS` に到達した場合は「引き分け！」となる。

---

## 5. バトル終了後

- 勝敗が決まるとオーバーレイが出る
- **「デッキを組みなおす」を押すとデッキは破棄され**、デッキ選択画面から選び直しになる
- 画面上部の「はじめから」ボタン、モード切替（1人プレイ ⇄ 2人交互プレイ）でも同じくリセットされる
- デッキは保存されない。毎回選び直す

---

## 6. デザイン

図鑑アプリとトーンを統一している。

| 項目 | 値 |
|---|---|
| フォント | M PLUS Rounded 1c（Google Fonts） |
| 背景 | `#e8e8e8` |
| カード | `#ffffff` / 角丸14px / やわらかい影 |
| 文字色 | `#333333`（薄い文字 `#666666`） |
| アクセント（プレイヤー1） | `#d5382f`（赤） |
| アクセント（プレイヤー2） | `#3b5ca8`（青） |
| HPバー | 緑 `#51cf66` → 残り35%未満で赤 `#ff6b6b` |

場のキャラは、デッキ選択画面と同じ**縦型カード**（絵が上、下に名前・HP・わざ）で表示する。

### スマホ表示（画面幅819px以下）

**対戦する2キャラを左右に並べて1画面に収める。** iPhone（375×812）で、キャラ2体のパネル下端が524px、メッセージ欄の下端が585px に収まり、Safariのアドレスバーを引いた実質の表示領域に全部入る。

あわせて次を圧縮している。

- ロゴ32px、選択メニューとボタンを1行にまとめる
- 名前13px / わざ10px / HPバー8px
- タップ案内を「🎲 ここをタップしてサイコロをふる」→「**🎲 タップ！**」に短縮
- ダメージ数字 46px → 34px
- ログ欄 150px → 110px

PC表示（820px以上）は従来のゆったりしたレイアウトのまま。

スマホ用のCSSは `<style>` の**最後**にまとめてある（後勝ちさせるため。途中に置くと基本スタイルに上書きされる）。

### アイコン

`icon_thum.png` を `apple-touch-icon`（180×180指定）とファビコンの両方に設定。ホーム画面に追加したときの名前は「カードバトル」。

---

## 7. 現在入っていないもの・今後の課題

- **アイテムカード**：かいふく薬・ちからの石・まもりの盾・しあわせコインといったアイテムは現在のバージョンでは削除。デッキはキャラのみ。後で追加する前提
- **バランス**：図鑑の入力値をそのまま使うため、キャラ間の性能差が非常に大きい（HP10のキャラとダメージ500のわざが同居する）。図鑑の入力が進んだらHP下限やダメージ上限の調整が必要になる可能性がある
- **取り合いドラフト**：使えるキャラが40体を超えたら、重複なしのドラフトに切り替えられる
